query GetNotifications($pagination: PaginationQueryInput) {
  getNotifications(pagination: $pagination) {
    hasNextPage
    hasPreviousPage
    limit
    page
    totalCount
    items {
      body
      createdAt
      id
      isRead
      meta
      receiverId
      title
      type
      updatedAt
    }
  }
}
